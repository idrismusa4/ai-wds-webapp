Flask==3.0.0
numpy==1.24.3 --only-binary :all:
opencv-python-headless==4.8.1.78  # Remove regular opencv-python as it's not needed with headless
torch==2.0.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu  # Use CPU-only version
torchvision==0.15.2+cpu --extra-index-url https://download.pytorch.org/whl/cpu  # Use CPU-only version
ultralytics==8.0.178
gunicorn==21.2.0
